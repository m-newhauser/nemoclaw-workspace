# Required: shared secret that authenticates requests to this local service.
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
# Set the same value as NEMOCLAW_TOKEN in your agent's environment.
NEMOCLAW_TOKEN=change-me-to-a-real-secret

# Optional overrides
# MODEL_ID=nvidia/gliner-PII
# THRESHOLD=0.5
